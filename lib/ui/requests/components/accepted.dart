import 'package:flutter/material.dart';

class AcceptedReservations extends StatefulWidget {
  const AcceptedReservations({super.key});

  @override
  State<AcceptedReservations> createState() => _AcceptedReservationsState();
}

class _AcceptedReservationsState extends State<AcceptedReservations> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
